[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Redirect all routing to index.html for React Router SPA (Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
